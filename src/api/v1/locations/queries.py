"""Database query functions for locations module.

This module is reserved for database query functions. Currently, the locations
module does not use local database queries as it relies on external Google Maps API.
"""
