services:
act:
  image: ghcr.io/catthehacker/ubuntu:act-latest
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - .:/workspace
  working_dir: /workspace
  entrypoint: bash -c "curl -s https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash && act ${ACT_ARGS:-}"
