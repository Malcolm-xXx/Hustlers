$kind: http-request
name: Buyer order summary request
url: "{{base_url}}/api/v1/orders/buyer/summary/"
method: GET
description: |-
  Fetch buyer spending summary.

  Auth: IsAuthenticated + Buyer role required.
  Query Param: range = "today" | "week" | "month"

  Success response: `200 OK`

  ```json
  {
    "total_spent": "1500.00",
    "total_orders": 15,
    "average_order_price": "100.00"
  }
  ```

  Common errors:
  - `400 Bad Request`: validation error response
  - `401 Unauthorized`: authentication required
  - `403 Forbidden`: permission denied
  - `404 Not Found`: resource does not exist
