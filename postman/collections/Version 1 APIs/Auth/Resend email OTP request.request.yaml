$kind: http-request
name: Resend email OTP request
url: "{{base_url}}/api/v1/auth/register/resend-email-otp/"
method: POST
body:
  type: json
  content: |-
    {
      "email": "{{email}}"
    }
order: 1776961561552
description: |-
  Resend the registration verification OTP.

  Auth: Public

  Request body:
  ```json
  {
  "email": "email"
  }
  ```

  Success response: `200 OK`

  ```json
  {
  "detail": "Verification OTP sent if account exists."
  }
  ```

  Common errors:
  - `400 Bad Request`: validation error response
  - `401 Unauthorized`: authentication required
  - `403 Forbidden`: permission denied
  - `404 Not Found`: resource does not exist
